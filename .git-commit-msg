Merge origin/main into feat/error-capture

Three conflicts, all real rather than textual:

- db.py: main took (A35) for the OAuth-grant backfill, which this branch had also
  used. Both migrations are needed and independent, so mine is renumbered (A36) —
  and the two doc references to it updated, since a stale number in a migration
  log is worse than none.
- api.py: an import line where each side added a different symbol. Both kept.
- scripts/usage_report.py: an add/add. Main committed the same script from another
  session while this branch was evolving it. Verified main's copy is a strict
  subset of this one — the only line it holds that differs is the same line without
  a trailing comma — so this branch's version wins with nothing lost.

The docs auto-merged; spot-checked that both sides survived rather than trusting it
(this branch's evidence-column and audit-hardening notes, main's OAuth-grant ones).

Full suite green on the merged tree: 1,653.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
